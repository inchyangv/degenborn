[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @degenborn/worker build:full"

[deploy]
startCommand = "node apps/worker/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
